# Native modules (node-pty) must match Electron's ABI, not Node's.
# prebuild-install reads these to download the prebuilt for Electron 29
# (ABI 121 = highest node-pty prebuilt available in @homebridge v0.13.1).
runtime=electron
target=29.4.6
build_from_source=false
