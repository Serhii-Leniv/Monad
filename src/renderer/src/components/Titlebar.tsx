/**
 * Invisible top drag-strip for the frameless window. The project switcher is a
 * separate floating bar (ProjectBar); the OS draws min/max/close at the top-right.
 */
export default function Titlebar(): JSX.Element {
  return <div className="titlebar" />
}
