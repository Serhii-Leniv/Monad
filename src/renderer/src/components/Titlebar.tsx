/**
 * Minimal top bar: just the window-drag region. The native min/maximize/close
 * buttons are drawn by the OS in the top-right (titleBarOverlay).
 */
export default function Titlebar(): JSX.Element {
  return <div className="titlebar" />
}
